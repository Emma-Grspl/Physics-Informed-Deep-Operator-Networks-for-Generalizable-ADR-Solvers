name: "benchmark_curriculum_t05"
seed: 0
seeds: [0, 1, 2]

training:
  t_max: 0.5
  batch_size: 4096
  learning_rate: 6.078744921577277e-05
  loss_weights:
    wr: 500.0
    wi: 100.0
    wb: 200.0

curriculum:
  warmup:
    iters: 5000
    log_every: 500
  phases:
    - name: "phase_t05"
      t_max: 0.5
      iters: 15000
      log_every: 1000

evaluation:
  n_cases_per_family: 20
  families:
    Tanh: [0]
    Sin-Gauss: [1]
    Gaussian: [3]

inference:
  batch_size: 10
  nx: 400
  nt: 200
  warmup_iters: 3

outputs:
  root_dir: "outputs/benchmarks"
